# Vectorized N-body simulation

Simulation of a dynamical system of particles interacting with each other gravitationally.

## Init project

### Requirements

- numpy
- matplotlib

1. Install using pip
```
   pip install -r requirements.txt
```

## Usage

```
   python nbody.py
```